# All NEXT_PUBLIC values are visible to visitors. Never put secrets here.
NEXT_PUBLIC_SITE_URL=https://seahorizonholidays.com
# Set to preview only for non-production builds.
NEXT_PUBLIC_DEPLOY_ENV=
# Optional verified Search Console property token (public).
GOOGLE_SITE_VERIFICATION=
# Existing analytics helpers are not enabled automatically.
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_META_PIXEL_ID=
# No credentials are needed for the WhatsApp enquiry flow.
